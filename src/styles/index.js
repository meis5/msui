import base from './base'
import container from './container'
import color from './color'
import flex from './flex'
import components from './components'

export default `${base}${container}${color}${flex}${components}`
